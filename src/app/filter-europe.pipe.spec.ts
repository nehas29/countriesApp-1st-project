import { FilterEuropePipe } from './filter-europe.pipe';

describe('FilterEuropePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEuropePipe();
    expect(pipe).toBeTruthy();
  });
});
