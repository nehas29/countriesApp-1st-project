import { FilterAmericaPipe } from './filter-america.pipe';

describe('FilterAmericaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAmericaPipe();
    expect(pipe).toBeTruthy();
  });
});
