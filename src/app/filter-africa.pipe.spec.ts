import { FilterAfricaPipe } from './filter-africa.pipe';

describe('FilterAfricaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAfricaPipe();
    expect(pipe).toBeTruthy();
  });
});
